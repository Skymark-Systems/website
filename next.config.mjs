/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['source.unsplash.com', 'images.unsplash.com', 'cdn.pixabay.com', 'images.pexel.com'],
	},
}

export default nextConfig
